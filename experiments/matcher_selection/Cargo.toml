[package]
name = "matcher-selection"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Prepare diverse, balanced query suites for matcher-selection experiments"

[dependencies]
lb-harness = { path = "../../harness", default-features = false }
clap = { version = "4.6.1", features = ["derive"] }
serde = { workspace = true }
serde_json = { workspace = true }
toml = "1.1.2"

[dev-dependencies]
tempfile = "3"
